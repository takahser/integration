version: '3.4'
services:
  provider-api:
    build:
      context: ./provider
      dockerfile: dev.dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./provider/:/usr/src/app
      - ./protocol/artifacts:/usr/src/app/artifacts
      - ./redspot/:/usr/src/redspot
      - ../data/:/usr/src/data
    environment:
      - CONTRACT_ADDRESS
      - DAPP_CONTRACT_ADDRESS
      - PROVIDER_ADDRESS
      - PROVIDER_MNEMONIC
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
    links:
      - substrate-node
      - mongodb
    extra_hosts:
      - "host.docker.internal:host-gateway"
  mongodb:
    image: mongo:5.0.4
    volumes:
      - ./mongodb/:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
    ports:
      - "27017:27017"
  substrate-node:
    build:
      context: ./docker/
      dockerfile: Substrate.dockerfile
    ports:
      - "9615:9615"
      - "9944:9944"
      - "9933:9933"